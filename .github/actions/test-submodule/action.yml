name: 'Run submodule tests'
description: 'Runs make run-test and supplies all necessary files.'

runs:
  using: "composite"
  steps:
      - name: "Test Build"
        shell: bash
        run: |
          echo "Test"
          ls -a
          echo "$GITHUB_ACTION_PATH"
          chmod +x $GITHUB_ACTION_PATH/workflow-run.sh
          bash $GITHUB_ACTION_PATH/workflow-run.sh
