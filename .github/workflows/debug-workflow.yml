on: push

jobs:
  test-job:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Workspace
        run: echo ${{ github.repository }}
      - name: Test actions
        uses: Janmtbehrens/OpenSlides/test-submodule/@latest